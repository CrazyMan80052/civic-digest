## 2024-05-18 - ARIA label to icon-only buttons
**Learning:** Components with only icons from libraries like `lucide-react` (such as `<X />`) need explicit `aria-label` attributes to be accessible to screen readers, especially when used in actionable elements like `<button>`.
**Action:** Always check icon-only buttons for `aria-label` during UX/accessibility reviews.
