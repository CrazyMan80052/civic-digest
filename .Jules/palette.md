## 2025-02-12 - [Title] \n**Learning:** In `ResidentMicroSurveyModal.tsx`, a custom tab UI was implemented using simple buttons. This lacked the necessary ARIA attributes to correctly identify the structure as a tablist to screen readers.\n**Action:** Applied standard a11y attributes (`role='tablist'`, `role='tab'`, `aria-selected`, `type='button'`) to ensure proper semantic meaning and state communication for assistive technologies. We will continue to look out for such micro-improvements.
